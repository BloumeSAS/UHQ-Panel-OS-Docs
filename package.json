{
  "name": "uhq-panel-os-docs",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev":   "vitepress dev --host 0.0.0.0 --port 3000",
    "build": "vitepress build",
    "preview": "vitepress preview --host 0.0.0.0 --port 3000"
  },
  "devDependencies": {
    "vitepress": "^1.3.0"
  }
}
